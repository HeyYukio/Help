1- Gerando ssh key no git bash:

	$ ssh-keygen -t rsa -b 4096 -C "yukiomrt@gmail.com"

	$ eval $(ssh-agent -s)

	$ ssh-add ~/.ssh/id_rsa

2 - Copiando a ssh key:

	$ clip < ~/.ssh/id_rsa.pub
