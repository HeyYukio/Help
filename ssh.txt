1- Gerando ssh key no git bash:

	$ ssh-keygen -t rsa -b 4096 -C "yukiomrt@gmail.com"

	$ eval $(ssh-agent -s)

	$ ssh-add ~/.ssh/id_rsa

2 - Copiando a ssh key:

	$ clip < ~/.ssh/id_rsa.pub

3 - Clonando uma única branch: 

	git clone --single-branch -b branchname git@gitlab.com:HeyYukio/pcs3539.git